export default function movementArgs(
    position,
    width,
    type,
    blockCount,
    length,
    direction
  ) {
    this.position = position;
    this.width = width;
    this.type = type;
    this.blockCount = blockCount;
    this.length = length;
    this.direction = direction;
  }
  